<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qianfeng.zhushou"
    android:versionCode="1"
    android:versionName="v1.2.4" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Light.NoTitleBar" >
        <activity
            android:name=".other.ui.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 引导页 -->
        <activity android:name=".other.ui.GuideActivity" />
        <!-- 主页面 -->
        <activity
            android:name=".other.ui.HomeActivity"
            android:windowSoftInputMode="stateHidden" />
        <!-- 礼包详情页面 -->
        <activity android:name=".gift.ui.GiftInfoActivity" />
        <!-- 登录页面 -->
        <activity android:name=".profit.ui.UserLogin" />
        <!-- 注册页面 -->
        <activity android:name=".profit.ui.UserRegist" />
        <!-- checkuser页面 -->
        <activity android:name=".profit.ui.UserLoginForgotPasswordCheckNum" />
        <!-- profitinfo -->
        <activity android:name=".profit.ui.ProfitGameInfoActivity" />
        <activity android:name=".exchange.ui.Exchangegiftinfo" >
        </activity>
        <activity android:name=".active.ui.ActiveInfo" />
        <activity android:name=".profit.ui.UserRegistCheckPhonenumber" >
        </activity>
    </application>

</manifest>
